const { Attr } = require( '../module' );

Attr.style( 'bold' );
console.log( 'Change the text style to bold' );
Attr.reset();

Attr.style( 'dim' );
console.log( 'Change the text style to dim' );
Attr.reset();

Attr.style( 'italic' );
console.log( 'Change the text style to italic' );
Attr.reset();

Attr.style( 'underline' );
console.log( 'Change the text style to underline' );
Attr.reset();

Attr.style( 'blinking' );
console.log( 'Change the text style to blinking' );
Attr.reset();

Attr.style( 'reverse' );
console.log( 'Change the text style to reverse' );
Attr.reset();

Attr.style( 'hidden' );
console.log( 'Change the text style to hidden' );
Attr.reset();

Attr.style( 'strikethrough' );
console.log( 'Change the text style to strikethrough' );
Attr.reset();

Attr.style( 'default' );
console.log( 'Change the text style to default' );
